# Render / local API (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:4000

# https://cloud.walletconnect.com — optional; without it, MetaMask / injected wallets still work
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
